---
description: For actions like running tests, migrations, and other actions that rely on python dependencies, use `uv run` to run them. E.g `uv run pytest ...` or `uv run alembic ...`
alwaysApply: false
---
